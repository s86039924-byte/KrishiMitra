# Copy to .streamlit/secrets.toml for local use, OR paste these into the
# Streamlit Community Cloud "Secrets" box (App → Settings → Secrets).
# Point this at your deployed backend (Render) URL — NO trailing slash.
BACKEND_URL = "https://krishimitra-backend.onrender.com"
BACKEND_TIMEOUT = "120"
